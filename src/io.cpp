#include "io.h"
#include "error.h"

int load(string name, string filename)
{
    cout << name << endl
         << filename << endl;
    return OK;
}

int store(string name, string filename)
{
    cout << name << endl
         << filename << endl;
    return OK;
}
